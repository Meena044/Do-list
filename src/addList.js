export function adddingNewList(){
    let todos = JSON.parse(localStorage.getItem("todosList")) || [];

    const listForm = document.getElementById('newProjectForm');
    listForm.addEventListener('submit', ()=>{
        
    });
}